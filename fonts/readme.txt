Fonts other than the one used by core (MS Mincho by default.)
To specify font, set "font" in a script file.
Include the font.woff file itself, as well as a script file specifying variables pertaining to text sizes and margins.
Fonts are always a little scuffed to work with, I apologize.