Only .mp3 files are supported, but this can be changed within /core/scripts.js.

To set the audio on a page, make sure the second class of the <head> tag matches the name of the .mp3 file.
Do not include the full path or the .mp3 extension.
It should be set to "noaudio" if no audio is to be used.